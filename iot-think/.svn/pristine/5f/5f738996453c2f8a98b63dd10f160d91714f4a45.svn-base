package com.rz.iot.think.model.param;

import lombok.Data;

/**
 * Author by xuxiake, Date on 2019/4/2.
 * PS: Not easy to write code, please indicate.
 * Description：
 */
@Data
public class IotScreenProgramAddParamSource {

    //显示在左上角的坐标
    public Integer left;
    public Integer top;
    //宽高
    public Integer width;
    public Integer height;
    //起始播放时间
    public Integer playTime;
    //持续播放时长
    public Integer timeSpan;
    //入场特效播放时长
    public int entryEffectTimeSpan;
    //出场特效播放时长
    public int exitEffectTimeSpan;
    //入场特效字符
    public String entryEffectStr;
    //出场特效字符
    public String exitEffectStr;

    // 行高（适用于天气、数字时钟、单行文本）
    private Double lineHeight;
    // html标签（适用于天气、数字时钟、单行文本）
    private String html;

    // 文字滚动速度（适用于单行文本）
    public int speed ;

    // 展示天气的城市（适用于天气）
    private String city;
    // 展示天气的城市代号（适用于天气）
    private Integer code;

    // 素材id
    private Integer materialId;
    // 素材类型 1：图片；2：视频（适用于视频、图片）
    private Integer materialType;
}
